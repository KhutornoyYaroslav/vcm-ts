DATASET:
  SEQUENCE_LENGTH: 7
  SEQUENCE_STRIDE: 1
  SUBDIR_INPUTS: ''
  METADATA_PATH: ''
  OD_GOP_SIZE: 32
  OD_STAGE: 5
  TEST_OD_ROOT_DIRS: []
  TEST_ROOT_DIRS: []
  TEST_SUBDIR_LISTS: []
  TRAIN_ROOT_DIRS: ['path/to/dir']
  TRAIN_SUBDIR_LISTS: ['path/to/txt']
  TYPE: SequenceDataset
DATA_LOADER:
  NUM_WORKERS: 8
  PIN_MEMORY: True
INPUT:
  IMAGE_SIZE: [256, 256]
  MAKE_DIVISIBLE_BY: 8
MODEL:
  ARCHITECTURE: DCVC_HEM
  DEVICE: cuda
  PRETRAINED_WEIGHTS: ''
OUTPUT_DIR: outputs/test
SOLVER:
  LAMBDAS: [85, 170, 380, 840]
  LR: 0.0001
  STAGES: [
    ['1', 'me', 'single', 'me', 'none', '1e-4', '1'],
    ['1', 'me', 'single', 'me', 'me', '1e-4', '3'],
    ['1', 'rec', 'single', 'rec', 'none', '1e-4', '3'],
    ['1', 'rec', 'single', 'rec', 'rec', '1e-4', '3'],
    ['1', 'all', 'single', 'rec', 'all', '1e-4', '6'],
    ['2', 'all', 'single', 'rec', 'all', '1e-4', '5'],
    ['4', 'all', 'single', 'rec', 'all', '1e-4', '3'],
    ['4', 'all', 'single', 'rec', 'all', '1e-5', '1'],
    ['4', 'all', 'cascade', 'rec', 'all', '5e-5', '2'],
    ['4', 'all', 'cascade', 'rec', 'all', '1e-5', '3']
  ]
TENSORBOARD:
  BEST_SAMPLES_NUM: 16
  WORST_SAMPLES_NUM: 16
